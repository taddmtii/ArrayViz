What needs to still be done:

- Implement Predict Mode
- general error checking
- Highlight statements/expressions within the UI as commands execute/undo. Highlights need to disappear when code is deleted/not being stepped through.

SECOND SEMESTER GOALS:
- Create preset options in the UI for auto populated code for the following sorts and searches:
  - Bubble Sort
  - Insertion Sort
  - Selection Sort
  - Quick Sort
  - Merge Sort
  - Heap Sort
  - Binary Search
  - Linear Search

- Expression support within f-strings
